export interface Cloud {
    id: number
    picture: string
    alt: string
    isCorrect: boolean
    title: string
    description: string
}

export interface Snowflake {
    id: number
    picture: string
    alt: string
    isCorrect: boolean
    title: string
}